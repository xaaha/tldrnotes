---
import { Image } from "astro:assets";
import { Icon } from "astro-icon/components";
import { slugify, formatDate } from "@scripts/utils";

export interface Props {
  frontmatter: Frontmatter;
  url: string | undefined;
  tagType?: "h1" | "h2" | "h3" | "h4" | "h5" | "h6";
}

const { frontmatter, url, tagType = "h2" } = Astro.props;
const HeadingTag = tagType;

const { title, date, description, image, category } = frontmatter;

const tags = category
  ? category.split(" ").filter((tag) => tag.trim() !== "")
  : [];
---

<article class="postcard">
  <!-- Optional Image Thumbnail -->
  {
    image?.src && (
      <a
        href={url}
        class="postcard__image-link"
        aria-hidden="true"
        tabindex="-1"
      >
        <Image
          src={image.src}
          alt={image.alt ?? `Thumbnail for ${title}`}
          width="130"
          height="130"
          format="webp"
          fit="cover"
          quality={80}
          aspectRatio="1:1"
        />
      </a>
    )
  }

  <!-- Main Content Area -->
  <div class="postcard__content">
    <HeadingTag class="postcard__title">
      <a href={url}>{title}</a>
    </HeadingTag>

    <div class="postcard__metadata">
      {
        date && (
          <>
            <Icon name="tabler:calendar-week" class="postcard__icon" />
            <time datetime={date}>{formatDate(date)}</time>
          </>
        )
      }
    </div>

    <p class="postcard__description">{description}</p>

    <!-- Tags Container -->
    {
      tags.length > 0 && (
        <div class="postcard__tags">
          {tags.map((tag) => (
            <a href={`/tags/${slugify(tag)}`} class="postcard__tag-badge">
              {tag}
            </a>
          ))}
        </div>
      )
    }
  </div>
</article>

<style>
  /* Base styles for the postcard container */
  .postcard {
    display: flex;
    align-items: flex-start;
    gap: 1.75rem;
    padding-block: 2.5rem;
    border-bottom: 1px solid hsl(var(--txt) / 0.1);
    text-decoration: none;
    color: hsl(var(--txt));
    transition: background-color 0.2s ease;
  }

  .postcard:hover {
    background-color: hsl(var(--muted) / 0.5);
  }

  /* Responsive layout for mobile: stack vertically */
  @media (max-width: 640px) {
    .postcard {
      flex-direction: column;
      gap: 1.25rem;
    }
  }

  /* THUMBNAIL IMAGE STYLES */
  .postcard__image-link {
    display: block;
    flex-shrink: 0;
  }

  .postcard__image-link :global(img) {
    width: 130px;
    height: 130px;
    object-fit: cover;
    border-radius: 0.5rem;
    background-color: hsl(var(--muted));
    transition: transform 0.3s ease;
  }

  .postcard:hover .postcard__image-link :global(img) {
    transform: scale(1.03);
  }

  @media (max-width: 640px) {
    .postcard__image-link :global(img) {
      width: 100%;
      height: 180px;
    }
  }

  /* CONTENT STYLES */
  .postcard__content {
    flex-grow: 1;
    display: flex;
    flex-direction: column;
  }

  /* Title Styles */
  .postcard__title {
    font-size: 1.375rem;
    font-weight: 600;
    line-height: 1.2;
    margin: 0;
  }

  .postcard__title a {
    text-decoration: none;
    color: inherit;
    background-image: linear-gradient(hsl(var(--accent)), hsl(var(--accent)));
    background-repeat: no-repeat;
    background-size: 0% 2px;
    background-position: 0% 100%;
    transition: background-size 0.3s ease;
  }

  .postcard__title a:hover {
    background-size: 100% 2px;
  }

  /* Metadata (Date) Styles */
  .postcard__metadata {
    font-size: 0.875rem;
    color: hsl(var(--txt) / 0.7);
    margin-block: 0.5rem 1rem;
    display: flex;
    align-items: center;
    gap: 0.4rem;
  }

  .postcard__icon {
    transform: translateY(-1px);
    font-size: 1.4em;
  }

  /* Description Styles */
  .postcard__description {
    margin: 0;
    /* Use the main text color with slightly less reduced opacity */
    color: hsl(var(--txt) / 0.85);
    line-height: 1.6;
    font-size: 1.145rem;
  }

  /* TAGS STYLES */
  .postcard__tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: 1.25rem;
  }

  .postcard__tag-badge {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 500;
    padding: 0.25em 0.75em;
    background-color: hsl(var(--muted));
    border-radius: 9999px;
    text-decoration: none;
    color: hsl(var(--txt) / 0.8);
    transition: all 0.2s ease;
  }

  .postcard__tag-badge:hover {
    background-color: hsl(var(--accent) / 0.2);
    color: hsl(var(--txt));
  }
</style>
